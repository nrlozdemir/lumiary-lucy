/*
 *
 * LibraryDetail reducer
 *
 */

import { fromJS } from "immutable";

export const types = {};
export const actions = {};
export const initialState = fromJS({});

const libraryDetailReducer = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

export default libraryDetailReducer;
