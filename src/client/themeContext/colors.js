const dark = {
  // global values
  bodyBackground: '#21243B',
  moduleBackground: '#373f5b',
  moduleShadow: 'rgba(0, 0, 0, 0.5)',
  duskBackground: '#545b79',
  textColor: '#ffffff',
  // global values

  // selectbox
  selectBackground: '#21243B',
  selectBorder: '#545b79',
  // selectbox

  // progressbar values
  progressBackground: '#21243b',
  progressColor: '#545b79',
  progressShadow: 'transparent',
  progressCountBackground: '#373f5b',
  // progressbar values

  // labels
  labelBackground: '#545b79',
  labelColor: '#ffffff',
  labelShadow: 'rgba(0, 0, 0, 0.5)',
  // labels

  // chart values
  chartBackground: '#21243b',
  chartLightBackground: '#545b79',
  chartShadow: 'rgba(0, 0, 0, 0.5)',
  chartStadiumBarBackground: '#21243b',
  chartStadiumBarBorder: '#545b79',
  chartStadiumCenterBackground: '#373f5b',
  // chart values

  // tabs values
  tabBackground: '#21243b',
  tabActiveBackground: '#545b79',
  tabBorder: '#545b79',
  // tabs values
}

const light = {
  // global values
  bodyBackground: '#e8ecf0',
  moduleBackground: '#f3f6f9',
  moduleShadow: '#bebebe',
  duskBackground: '#ffffff',
  textColor: '#505050',
  // global values

  // selectbox
  selectBackground: '#ffffff',
  selectBorder: '#c6c9d7',
  // selectbox

  // progressbar values
  progressBackground: '#ffffff',
  progressColor: '#3dd6c4',
  progressShadow: '#bebebe',
  progressCountBackground: '#ffffff',
  // progressbar values

  // labels
  labelBackground: '#ffffff',
  labelColor: '#505050',
  labelShadow: 'rgba(0, 0, 0, 0.5)',
  // labels

  // chart values
  chartBackground: '#e8ecf0',
  chartShadow: '#bebebe',
  chartLightBackground: '#ffffff',
  chartStadiumBarBackground: '#e8ecf0',
  chartStadiumBarBorder: '#c6c9d7',
  chartStadiumCenterBackground: '#f3f6f9',
  // chart values

  // tabs values
  tabBackground: '#e8ecf0',
  tabActiveBackground: '#ffffff',
  tabBorder: '#c6c9d7',
  // tabs values
}

const themes = {
  light,
  dark,
}

export { themes }
